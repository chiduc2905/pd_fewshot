# PD Scalogram Project Configuration

# This file serves as a reference for default hyperparameters.
# Actual training/testing is controlled via command line arguments in main.py.

## Dataset Configuration
# DATASET_PATH = ./scalogram_images/
# CLASSES = ['surface', 'corona', 'no_pd']
# NUM_CLASSES = 3
# SPLIT = 40/class for val + 40/class for test, rest for train

## Training Configuration
# MODEL = covamnet  # choices: covamnet, protonet, cosine, matchingnet, relationnet
# DEVICE = cuda
# NUM_EPOCHS = 100 (1-shot) or 70 (5-shot)
# LEARNING_RATE = 0.0001
# BATCH_SIZE = 1  # Episodes per batch

## Few-Shot Settings
# WAY_NUM = 3    # Number of classes per episode
# SHOT_NUM = 1   # Support samples per class (1 or 5)
# QUERY_NUM = 1  # Query samples per class
# EPISODE_NUM_TRAIN = 100
# EPISODE_NUM_VAL = 100
# EPISODE_NUM_TEST = 150

## Output
# PATH_WEIGHTS = checkpoints/
# SEED = 42
